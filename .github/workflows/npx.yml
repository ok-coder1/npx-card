name: npx okcoder1

on:
  push:
    branches: [ "master" ]

jobs:
  npx:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          cache: 'npm'

      - run: npx okcoder1
